import * as React from "react";
import { Typography } from "@mui/material";

type SkillGroupsProps = {};

export function SkillGroups({}: SkillGroupsProps) {
  return <Typography>SkillGroups</Typography>;
}
